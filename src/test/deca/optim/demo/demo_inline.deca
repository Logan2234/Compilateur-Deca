class A {
    int myFct(int x, int y) {
        return  y*2 - x*y -4;
    }
}

{
    A a = new A();
    int x = readInt();
    int y = readInt();
    x = a.myFct(x,y);
    y = a.myFct(y,x);
    y= a.myFct(1,8);
    print(x,y);
}