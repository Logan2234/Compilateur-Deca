// Description:
//    regarder comment marche une double minus
//
// Resultats:
//
//
// Historique:
//    cree le 19/01/2023

class A {
    int m = 31*m;
    boolean get(){
        return 31*m < 23*m;
    }

    int set(int a){}
}

class B extends A {
    int n = set(31*m) + 31*m;
}


{
    int a;
    int m = 15*a;
    int n = 63*a+2;

    60*m;
    
    -60*m;
    
    m = 10*m;
    
    -(10*m);
    
    if (15*m < 15*m){
        15*m;
    } else {
        15*m;
    }

    15*m + 63*m + 1;
    15*m - 15*m;
    (15*m) * (15*m);
    (15*m) / (15*m);
    (15*m) % (15*m);
    while (15*m < 15*m + (-(15*m))) {
        15*m;
    }

    print(15*m, 63*m, m*15);

    !(m*15 + 25*m - (20*m) / (64*m) * (32*m) % (1*m) == 0);
}
