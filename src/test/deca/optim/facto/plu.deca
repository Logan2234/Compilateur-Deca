// Description:
//    factoriser les plus en fois puis en switch
//
// Resultats:
//
//
// Historique:
//    cree le 20/01/2023


{
    int m;
    int n;
    int z;
    int a, b;

    //(m + m) + (m + m);
    //    15*m - 15*m;
    //m*n+1+2+5;
    //8 * m + n*m + z*m + z + a * z + a * b +m * n;
    //if (m + m < m + m +m){
    //    m + m + m * 8;
    //}
    //else {
    //    m*z+m*z;
    //}
    !(m*15+ 25*m - (20*m) / (64*m) * (32*m) % (1*m) == 0);
    
}